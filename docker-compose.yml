services:
  api:
    build:
      context: .
      dockerfile: src/Olga.Nlp.Api/Dockerfile
    ports: ["8080:8080"]
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      EmbeddingProvider: Fake
